import * as React from 'react';
import { useState } from 'react';

export const FavouriteBeers = () => {
  const [favouriteBeersList, setFavouriteBeersList] = useState([]);


  return <div></div>
}